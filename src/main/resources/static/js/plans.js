(async()=>{if(!document.getElementById('plansBody'))return;const rows=await api.get('/admin/api/plans');plansBody.innerHTML=rows.length?rows.map(r=>`<tr><td>${r.id}</td><td><strong>${r.name}</strong><div class='field-help'>${r.description||''}</div></td><td><strong>$${Number(r.price).toFixed(2)}</strong></td><td><span class='badge'>${r.billingPeriod}</span></td><td><span class='badge ${r.isActive?'active':'inactive'}'>${r.isActive?'Active':'Inactive'}</span></td></tr>`).join(''):`<tr><td colspan='5'><div class='empty-state'>No plans configured yet.</div></td></tr>`})();
async function createPlan(){const p={name:planName.value,description:planDescription.value,price:+planPrice.value,billingPeriod:billingPeriod.value,isActive:planActive.checked};await api.post('/admin/api/plans',p);location.reload()}
