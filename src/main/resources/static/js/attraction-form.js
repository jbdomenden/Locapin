(async()=>{const f=document.getElementById('attractionForm');if(!f)return;const areas=await api.get('/admin/api/areas');areaId.innerHTML=areas.map(a=>`<option value='${a.id}'>${a.name}</option>`).join('');const id=location.pathname.split('/')[3];if(id&&id!=='new'){const a=await api.get('/admin/api/attractions/'+id);window._id=id;areaId.value=a.areaId;['name','description','highlights','latitude','longitude','openHours'].forEach(k=>document.getElementById(k).value=a[k]||'')}f.addEventListener('submit',async(e)=>{e.preventDefault();const p={areaId:+areaId.value,name:name.value,description:description.value,highlights:highlights.value,latitude:+latitude.value,longitude:+longitude.value,openHours:openHours.value||null};window._id?await api.put('/admin/api/attractions/'+window._id,p):await api.post('/admin/api/attractions',p);location.href='/admin/attractions'})})();
